name = "specauto-ai"
compatibility_date = "2024-04-03"

[assets]
directory = "ui/dist"

# Note: The backend (server.py) is a Flask app and currently runs locally.
# To connect to it from a web-hosted UI, you would need to host server.py 
# on a public server (e.g., Render, Heroku) and update the API URL in App.jsx.
